<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/Dunkelrot_transparent</item>
        <item name="colorPrimaryDark">@color/Tintenblau</item>
        <item name="colorAccent">@color/Schwarz</item>
        <item name="android:textColorHint">@color/Schwarz</item>
        <item name="android:colorControlActivated">@color/Schwarz</item>


    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
